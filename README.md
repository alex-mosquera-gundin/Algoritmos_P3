## Comandos de Makefile para la compilacion y ejecucion del codigo:  ## 
**make**: compila el programa main.c con las flags -g, -Wall, etc ... <br/>
**make clean**: elimina los archivos .o generados por el compilador y el ejecutable.
